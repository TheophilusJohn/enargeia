# Local development serves the weights from public/models, which is gitignored.
# Production overrides this in .env.production — the only value that differs between them.
VITE_MODEL_URL=/models/qwen2.5-0.5b.enargeia
